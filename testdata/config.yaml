receivers:
  nop:

processors:
  filter_span:
    start_time_offset: 3600000000000

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers:
        - nop
      processors:
        - filter_span
      exporters:
        - nop
